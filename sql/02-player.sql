CREATE TABLE player (
                        id INTEGER PRIMARY KEY AUTOINCREMENT,
                        name TEXT NOT NULL,
                        team TEXT NOT NULL,
                        number INTEGER,
                        position TEXT
);